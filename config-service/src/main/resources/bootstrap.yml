server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andreaCards/microservices.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

